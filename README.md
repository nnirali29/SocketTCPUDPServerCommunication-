# SocketTCPUDPServerCommunication-
In this project I have implemented a socket program which communicates between 2 server’s, one is TCP concurrent server and other is UDP by using multi-threading. 
